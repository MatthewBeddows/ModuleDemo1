def run_test():
    """
    Simple test function that can be run on any device with Python installed.
    Returns a test completion message.
    """
    return "Test completed successfully!"

if __name__ == "__main__":
    # This allows the script to be run directly or imported as a module
    result = run_test()
    print(result)